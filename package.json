{
  "name": "zedocs-monorepo",
  "private": true,
  "engines": {
    "node": ">=10",
    "yarn": "^1.17.3"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "lint": "wsrun -mc lint",
    "build": "wsrun -temc build",
    "test": "wsrun -emc test"
  },
  "dependencies": {
    "wsrun": "^5.2.4"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^4.11.1",
    "@typescript-eslint/parser": "^4.11.1",
    "eslint": "^7.16.0",
    "prettier": "^2.2.1",
    "rimraf": "^3.0.2"
  }
}
